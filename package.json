{
  "name": "calitur",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "init": "jupyter book init --write-toc",
    "start": "jupyter book start",
    "build": "jupyter book build --html",
    "build:strict": "jupyter book build --html --strict",
    "build:pdf": "jupyter book build --pdf",
    "build:pdf:strict": "jupyter book build --pdf --strict",
    "clean": "jupyter book clean --all -y",
    "myst": "myst build --html",
    "myst:strict": "myst build --html --strict"
  },
  "dependencies": {
    "jupyter-book": "^2.1.2",
    "mystmd": "^1,6.6",
    "myst-directives": "^1.6.4",
    "myst-roles": "^1.6.4",
    "unist-builder": "^4.0.0"
  }
}
